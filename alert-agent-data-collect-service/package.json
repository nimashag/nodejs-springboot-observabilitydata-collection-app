{
  "name": "alert-agent-data-collect-service",
  "version": "1.0.0",
  "description": "Central Alert Data Collection Service - Aggregates alert data from all services for AATA",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && concurrently --kill-others --names \"ML,AATA\" --prefix-colors \"cyan,green\" \"npm run start:ml\" \"npm run start:node\"",
    "start:ml": "cd ml-module && python ml_service.py",
    "start:node": "node wait-for-ml.js",
    "start:aata-only": "npm run build && node dist/index.js",
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc",
    "collect": "node dist/index.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "concurrently": "^9.2.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
